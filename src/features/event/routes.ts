// import express from "express";

// import { getAllEvent, getNearByEvent } from "./controller";

// import { subRoutes } from "../../utils/constants";
// import HandleErrors from "../../middleware/handleError";

// const eventRoutes = express.Router();

// eventRoutes.get(subRoutes.GET_NEAR_BY_EVENTS, HandleErrors(getNearByEvent));
// eventRoutes.get(subRoutes.GET_ALL_EVENT, HandleErrors(getAllEvent));

// export default eventRoutes;
